const themePreference = useSelector(selectTheme);
 ${themePreference === "dark" && "dark"}

 Aditions:

 Improvements: 
 Multiple Labels Selection per Snippet

Fixes:
 create first file within editor
 Limit lenght of Files, Tags on Snippet Save Screen

 Language change of syntax highlighter

 new created label dont register

 